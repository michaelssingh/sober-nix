diff -Naur a/internal/api/allanime_enhanced.go b/internal/api/allanime_enhanced.go
--- a/internal/api/allanime_enhanced.go	2026-06-15 19:03:53.066251438 +0000
+++ b/internal/api/allanime_enhanced.go	2026-06-15 19:04:05.898125222 +0000
@@ -98,7 +98,10 @@
 		return "", nil, fmt.Errorf("could not extract anime ID from URL: %s", anime.URL)
 	}
 
-	mode := "sub" // Default
+	mode := "sub"
+	if util.GlobalAnimeMode != "" {
+		mode = util.GlobalAnimeMode
+	}
 	if quality == "" {
 		quality = "best"
 	}
diff -Naur a/internal/handlers/media.go b/internal/handlers/media.go
--- a/internal/handlers/media.go	2026-06-15 19:03:53.050251438 +0000
+++ b/internal/handlers/media.go	2026-06-15 19:04:02.730156004 +0000
@@ -184,6 +184,7 @@
 	if modeIdx == 1 {
 		mode = "dub"
 	}
+	util.GlobalAnimeMode = mode
 
 	streamURL, metadata, err := mh.GetAnimeStreamURL(anime, episodeNum, mode)
 	if err != nil {
diff -Naur a/internal/util/util.go b/internal/util/util.go
--- a/internal/util/util.go	2026-06-15 19:03:53.038251438 +0000
+++ b/internal/util/util.go	2026-06-15 19:04:00.254135413 +0000
@@ -382,6 +382,9 @@
 // Global variable to store download request
 var GlobalDownloadRequest *DownloadRequest
 
+// Global variable to store selected anime mode (sub vs dub)
+var GlobalAnimeMode string = "sub"
+
 // Global variable to store upscale request
 var GlobalUpscaleRequest *UpscaleRequest
 
@@ -397,6 +400,7 @@
 
 	// Define flags
 	debug := fs.Bool("debug", false, "enable debug mode")
+	dub := fs.Bool("dub", false, "play or download English dubbed version for anime")
 	perf := fs.Bool("perf", false, "enable performance profiling")
 	help := fs.Bool("help", false, "show help message")
 	altHelp := fs.Bool("h", false, "show help message")
@@ -454,6 +458,9 @@
 	// Store global configurations
 	GlobalSource = *sourceFlag
 	GlobalQuality = *qualityFlag
+	if *dub {
+		GlobalAnimeMode = "dub"
+	}
 	GlobalMediaType = *mediaTypeFlag
 	GlobalSubsLanguage = *subsLanguageFlag
 	GlobalAudioLanguage = *audioLanguageFlag
